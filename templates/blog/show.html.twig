{% extends 'base.html.twig' %}

{% block title %}Show{% endblock %}

{% block body %}
    <div class="container">
        <div class="row justify-content-center">
            {% if article is null %}
                <h1>Aucun article ne porte ce nom.</h1>
            {% else %}
        </div>
        <div class="row justify-content-center">
            <h4>Titre : {{ article.title }}</h4> <br />
        </div>
        <div class="row justify-content-center">
            <p>Description : {{ article.content }}</p> <br />
        </div>
        <div class="row justify-content-center">
            <p>Catégorie : {{ article.category.name }}</p>
        </div>
            {% endif %}

        <div class="row justify-content-center">
            <a href="{{ path('article_edit', { 'id': article.id }) }}"><button type="button" class="btn btn-warning mr-5">Modifier l'aticle</button></a>

            {{ include('article/_delete_form.html.twig') }}
        </div>
        </div>
    </div>
{% endblock %}
